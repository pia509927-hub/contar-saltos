input.onButtonPressed(Button.A, function () {
    let SALTOS = 0
    fijar_saltos += 1
    basic.showNumber(SALTOS)
})
let fijar_saltos = 0
basic.forever(function () {
	
})
basic.forever(function () {
	
})
